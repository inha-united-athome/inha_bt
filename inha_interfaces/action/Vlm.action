# Goal
int32 mode                 # 1=text, 2=live_image+prompt, 3=context_json
string text                # heard_text / user utterance (optional)
string prompt              # instruction/system directive (optional)
string context_json        # mode3에서 주로 사용 (optional)
int32 timeout_ms           # 0=inf

---
# Result
bool success
string generated_text
string result_json
string error_message

---
# Feedback
string state
float32 progress
